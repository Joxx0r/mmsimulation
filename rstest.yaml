apiVersion: v1
kind: ReplicationController
metadata:
  name: frontend
spec:
  replicas: 3
  selector:
    app: frontend
  template:
    metadata:
      name: frontend
      namespace: mm
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: joxxorr/frontend:latest      
